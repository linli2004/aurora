# AUR-DEMO-PACK-04 Review

## Intended acceptance

- `./scripts/run_demo.sh` opens presentation mode without requiring F11
- `./scripts/run_demo_windowed.sh` keeps the development workflow
- no presentation helper overlaps Music Space controls
- real artwork uses the full recognition-safe image
- generated Crystal identity no longer depends on an oversized single glyph
- `demo_doctor.sh` validates the installed runtime
- `package_demo.sh` produces a versioned archive and SHA-256 file

## Product result

Passing this pack freezes Aurora as Demo 1.0. Further work moves to Alpha architecture rather than adding more demo-only UI.
